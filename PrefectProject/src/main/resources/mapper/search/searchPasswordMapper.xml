<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.pcwk.ehr.search">
    <!-- 이메일 확인 -->
        <select id="emailCheck" parameterType="UserVO" resultType="int">
           SELECT COUNT(*) cnt         
           FROM users                
           WHERE email = #{email}
        </select>
    <!-- update new password -->
        <select id="updatePw">
           update users set password = #{password} where email = #{email}
        </select>
        <select id="readMember" parameterType="UserVO" resultType="UserVO">
            SELECT *
            FROM users
            WHERE email = #{email}
        </select>
</mapper>